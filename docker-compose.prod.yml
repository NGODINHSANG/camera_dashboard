version: '3.8'

services:
  backend:
    image: camera-dashboard-backend:v2.0
    container_name: camera-dashboard-backend
    restart: unless-stopped
    environment:
      - PORT=8080
      - DATABASE_PATH=/app/data/dashboard.db
      - JWT_SECRET=${JWT_SECRET:-change-this-secret}
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost}
      - MEDIAMTX_HOST=http://camera-nginx-proxy # Use nginx proxy for HLS streams
      - HLS_CACHE_PATH=/app/hls-cache # Local disk for HLS segments (avoids SMB reads)
    volumes:
      - ./data:/app/data
      - /mnt/win_recordings:/app/recordings # SMB mount - MP4 gốc trên Windows
      - /mnt/hls-cache:/app/hls-cache # LOCAL disk - HLS segments cho playback
      # Mount host directories for folder browser and recording
      - /home:/mnt/home
      - /opt:/mnt/opt
      - /var:/mnt/var
    networks:
      - app-network
    dns:
      - 127.0.0.11 # Docker's embedded DNS server
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost:8080/api/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    image: camera-dashboard-frontend:v2.0
    container_name: camera-dashboard-frontend
    restart: unless-stopped
    environment:
      - VITE_API_URL=/api
    depends_on:
      - backend
    networks:
      - app-network

  # Main Nginx Reverse Proxy (ONLY exposed port)
  nginx-proxy:
    image: nginx:alpine
    container_name: camera-nginx-proxy
    restart: unless-stopped
    ports:
      - "8081:80" # Router forward 54543 → 8081 (ONLY port exposed)
    volumes:
      - ./nginx/nginx-proxy.conf:/etc/nginx/nginx.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway" # Access MediaMTX on host network
    depends_on:
      - frontend
      - backend
    networks:
      - app-network
    healthcheck:
      test: [ "CMD", "wget", "-q", "--spider", "http://localhost/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # MediaMTX (RTSP/HLS/WebRTC streaming server)
  mediamtx:
    image: bluenviron/mediamtx:latest
    container_name: camera-mediamtx
    restart: unless-stopped
    ports:
      - "1935:1935" # RTMP ingestion (Router: 54541 → 1935)
    environment:
      - MTX_PROTOCOLS=tcp
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  nginx-cache:
    driver: local
